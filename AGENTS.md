# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Every completed application change must be committed, pushed to the connected GitHub repository, and deployed to the linked Vercel production project after validation; do not leave completed app changes only in the local worktree.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Product decisions

- The selected visual target is option 2 from the 27 July 2026 ideation set: a dense fleet ledger with a right-side vehicle inspector.
- The operating fleet has exactly five vehicles: three professional and two particular.
- Every vehicle has two assigned drivers; the fleet has ten unique drivers.
- Professional vehicles receive two WhatsApp shift reports per day. Each report records driver, start and accumulated odometer, kilometres driven, fuel litres, cost, timestamp, extraction confidence, and any consumption anomaly.
- Particular vehicles do not require per-shift reporting; either assigned driver can submit a shared odometer reading.
- Vehicle detail defaults to a collapsible shift ledger so the manager can audit each daily turn without leaving the fleet screen.
- The main ledger omits separate Vehicle and Status columns. The model remains visible below the plate, while Status is replaced by a numeric remaining-kilometres countdown to the next service.
- The Vehículos ledger labels the accumulated vehicle odometer column “Km totales”, never “Km acumulados”.
- Driver names are interactive selectors. Selecting either driver must update that row and the inspector with the driver's kilometres and fuel spend in euros for the day.
- The main ledger has no Uso column. Facturación appears immediately after Conductores.
- Driver selection must update daily billing, monthly accumulated billing, monthly accumulated trips, and the portion collected in cash today. Particular drivers display zero billing and zero trips unless they are later assigned commercial activity.
- The ledger includes a Taller column with the latest maintenance amount and concept. Selecting it navigates to the selected vehicle's dated workshop history inside Mantenimiento.
- The first release is a functional front-end prototype with realistic local data; WhatsApp, email, authentication, and persistence remain simulated. Document capture uses server-side OpenAI structured extraction when `OPENAI_API_KEY` is configured.
- The authorized Funes Motorsport import publishes only a structured summary (plate, date, document type, concepts, amounts, kilometre reading when present, source filename, and document number); original PDFs and personal data are not versioned. It contains 74 records across the five current fleet plates, while 1891 KPK remains separated as an unassigned record. The maintenance-annotations PDF stays pending until it can be identified unambiguously.
- Each authorized Funes Motorsport record with an available invoice, budget, or repair document also keeps an optimized first-page image preview in `public/funesmotorsport`; the preview opens from the associated maintenance invoice detail without exposing the original PDF.
- The web client is an installable PWA. Production must ship a linked manifest, 192px and 512px install icons, maskable icons, `display: standalone`, a root-scoped service worker, and an in-app install action when the browser exposes the install prompt.
- The product name is SOBRE RUEDAS and the primary interface language is Spanish. Its company/project logo is the exact user-supplied red-and-white wheel artwork stored at `public/brand/sobre-ruedas-logo.png`; the desktop home control uses that image without reinterpretation, while the phone home control and installable PWA icon use a centered square, safe-area adaptation stored at `public/brand/sobre-ruedas-app-icon.png`.
- The phone launcher, favicon, Apple touch icon, manifest icons, maskable icons, and manifest shortcuts must always use the red SOBRE RUEDAS wheel artwork, never the legacy T icon. Icon URLs are versioned whenever the artwork changes so installed mobile clients fetch the replacement.
- Every destination is a real application window. Flota, Lecturas, Facturas, Mantenimiento, Automatizaciones, Ajustes, and Ayuda must change the route hash, title, content, and active navigation state, but they are entered only from SOBRE RUEDAS controls.
- There is no permanent sidebar. The unnamed SOBRE RUEDAS summary is the sole navigation hub. Its first quick-action row shows only Vehículos and Facturas; Lecturas must not appear in that row. The persistent tyre-logo button always returns to this summary, including from an open KPI detail, and restores the combined four-colour chart with four grouped bars per vehicle.
- Vehículos is the single consolidated operational workspace. The former Vehículos, Facturación, Combustible/Repostaje, and Consumo mensual por vehículo entry points route to one `#/flota` screen using the Combustible banner-and-ledger pattern, with vehicle operations, monthly billing, refuelling history, and monthly consumption shown together. A legacy `#/gasolina` link resolves to Vehículos.
- Product hierarchy follows the selected dense-ledger direction with stronger readable type, elevated KPI cards, a persistent operational table, and an optional right-side inspector. On mobile, the inspector opens only after a vehicle is selected.
- Core review actions use accessible dialogs, visible focus states, semantic tables, named controls, and Escape-to-close behavior.
- Taller is integrated into Mantenimiento and is reached from the General quick actions. It offers a vehicle selector, full history, repeated concepts, and a photo-to-invoice action that preselects the current vehicle.
- Facturas created from a workshop photo are kept locally between reloads and immediately update Facturas, the selected vehicle's maintenance history, its July workshop expense, and its calculated profit margin.
- The vehicle inspector contains Actividad, Mantenimiento, Gasolina, and Gastos. Mantenimiento uses a compact Excel-like grid with one row for each agreed maintenance concept, the latest date, and a control to view the associated invoice document. Gasolina lists each driver's daily litres and fuel spend for the selected vehicle.
- Vehículos is a full fleet page, not only an inspector tab. It shows the current month's vehicle totals, monthly billing, accumulated litres and spend for all five vehicles, followed by the operational ledger, the selected vehicle's dated refuelling ledger, and the integrated billing workspace. Professional refuellings are assigned automatically to a driver from the recorded time and the vehicle's configured shift schedule.
- The Vehículos workspace uses the selected mobile reports visual language: a black global top bar over the teal report canvas, no visible report-tab row or standalone operational-entry/date row, stacked horizontal vehicle banners, anchored month selector, and a combined vehicle/facturación/consumo ledger. The tyre-logo button returns to General. Repostaje and Facturación are sections of Vehículos rather than separate entry points.
- The dashboard formerly called Informes is the default "SOBRE RUEDAS" home view for the root URL and installed PWA. It opens on General and shows SOBRE RUEDAS in the top bar. The Vehículos quick action is the single entry point for the combined vehicle workspace.
- Every root launch is normalized to `#/informes`. A standalone PWA launch always resets to SOBRE RUEDAS > General even when the previous session closed on another route; direct non-standalone deep links remain available.
- The global top bar is black with the red SOBRE RUEDAS wheel and white title. The notification bell remains visible, while a three-line menu reveals the Automatizaciones, Ajustes, and Ayuda icons.
- The SOBRE RUEDAS home dashboard has four financial metrics: Facturación in light green, Mantenimiento in orange, Combustible in red, and Neto in green. Their totals must represent monthly driver billing, workshop spend, recorded fuel spend, and billing minus all vehicle expenses respectively.
- General uses one elongated blue Conductores button that opens the driver analytics workspace; Mantenimiento is the second elongated button and Neto appears below it. Facturas is available from the black top bar immediately to the left of the notification bell.
- The grouped Vehículos button is the only dashboard entry point for the combined vehicle, billing, and refuelling workspace. The duplicate Repostaje, Ingreso, Conductores, and Combustible quick actions remain removed, and the General dashboard fits the viewport without vertical page scrolling.
- The orange Mantenimiento button is the only visible entry point for the complete maintenance workspace from General. The duplicate Mantenimiento quick action is removed.
- The Neto button opens a dedicated modal for the three professional vehicles, showing each vehicle's billing, the requested expense concepts, the resulting billing-minus-expenses amount, and the combined net total; the chart's Neto filter remains available separately.
- Neto vehicle cards omit the "COCHE PROFESIONAL" eyebrow and the "Facturacion - gastos" result label; the net amount and total expenses remain visible.
- The Neto detail uses compact per-vehicle summaries so the numbers remain scannable: expense rows stay collapsed behind “Ver gastos”, while each professional vehicle offers an inline manual concept/amount form. Manual entries persist locally for the selected reporting period, can be removed, and update the vehicle and combined net immediately.
- The phrase "Control de flota" is not used in the interface. The Flota workspace is headed simply "Vehículos".
- Vehicle order is fixed everywhere: 1) 5043 MLC, professional, Toyota Corolla; 2) 5750 MJV, professional, Toyota Corolla; 3) 5754 MJV, professional, Toyota Corolla; 4) 0344 LCP, particular, Lexus IS 300h; 5) 9401 LTG, particular, Peugeot 2008.
- Professional schedules are 5043 MLC (Mauricio 07:00–19:00, Amin 19:00–07:00), 5750 MJV (Tirso 06:00–18:00, Alex 18:00–06:00), and 5754 MJV (Andrés 04:00–16:00, Fernando 16:00–04:00); driver profile names from Administración are the source of truth when assigned profiles exist.
- The maintenance concept grid contains Aceite y filtro, Filtro habitáculo, Filtro de aire, Neumáticos, Pastillas de freno, Discos de freno, Transmisión, Bomba de agua, Bujías, Aceite de caja de cambios, Limpiaparabrisas, Fundas de asientos, and Varios.
- Every concept listed under a dated maintenance event's "Trabajos realizados" is interactive. Selecting it expands an inline table containing every exact normalized match for that concept across all dated work records for the currently selected vehicle, including date, odometer, concept, and amount.
- Mantenimiento opens with five stacked horizontal vehicle banners in fixed fleet order. Each banner shows the real Toyota, Lexus, or Peugeot brand mark before the plate and model. Selecting a banner reveals that vehicle's interventions ordered newest-first with dates formatted YYYY/MM/DD. The date control independently expands the work performed, while a separate invoice control opens the workshop invoice when one is available.
- Gastos shows vehicle-specific amounts for leasing coche, préstamo licencia, gasolina, taller, seguridad social, nómina, comisiones conductor, impuestos trimestrales, IVA intracomunitario, seguro, limpieza coche, and varios.
- Gastos calculates each vehicle's monthly profit margin as the combined monthly billing of its two drivers minus every expense assigned to that vehicle. The UI shows both driver subtotals, total billing, total expenses, absolute margin, and margin percentage.
- The General report chart uses four grouped bars per vehicle. Each group exposes the four established color-coded metrics: Facturación (light green), Mantenimiento (orange), Combustible (red), and Neto (green), while the grouped dashboard buttons preserve the operational routes and the Neto detail interaction.
- Uber Developer integration is server-only: UBER_CLIENT_ID, UBER_CLIENT_SECRET, UBER_REDIRECT_URI, and UBER_SCOPES are environment variables; secrets are never rendered in the browser, stored in localStorage, or committed. The connection uses Uber Driver API OAuth and only requests partner.accounts, partner.payments, and partner.trips. A diagnostics action reports endpoint availability and counts without returning bearer tokens or personal payloads.
- The public Uber privacy-policy route is /privacidad. It documents the requested Uber data, purposes, server-side retention, sharing, deletion rights, contact email, and OAuth disconnection. Settings exposes the same policy link and a server-side disconnect action clears the encrypted OAuth session cookie.
- The General chart header shows only “Resumen general por coche”; it does not show an explanatory subtitle below the title.
- Vehicle registration plates shown along the bottom axis of the General, Mantenimiento, Combustible, and Neto charts use bold type; driver names in the Facturación chart retain regular weight.
- The Ingreso report keeps exactly four concepts visually grouped in a centered compact table: Conductor, Vehículo, Viajes, and Ingreso mensual. Turno and its schedule must not appear in this view.
- Driver names throughout the Facturación workspace use uppercase text and a larger type size than the surrounding vehicle, trip, schedule, and revenue details.
- Every professional driver name in Facturación is an interactive selector. Selecting one opens an inline Monday-first calendar synchronized with the Facturación month and year controls; every billed day shows its daily revenue directly below the date, and the daily amounts add up exactly to that driver's monthly billing.
- Each professional vehicle plate in Facturación is also an interactive selector. Selecting it reveals the combined monthly billing of its two associated drivers, with each driver's subtotal and the vehicle total.
- The Facturación workspace header shows only the centered light-green title “Facturación”; the previous two-line income heading and explanatory subtitle are removed.
- Mantenimiento separates its five vehicle banners from the selected vehicle history with a black horizontal divider.
- When a KPI detail is open, the top bar becomes a compact title bar containing only the SOBRE RUEDAS wheel control and the centered name of the active detail: Mantenimiento, Facturación, Combustible, or Neto.
- The Ajustes action bar containing Descartar and Guardar ajustes remains fixed to the bottom of the viewport while the settings content scrolls, with reserved page space so it never covers the last controls.
- The Repostaje detail ledger uses this exact column order: Fecha, Hora, Conductor, Importe, Precio/Litro, and Factura. Turno and Litros do not occupy separate columns. Every entry includes a button that opens its locally archived Plenergy invoice, representing a document downloaded from the fleet's Plenergy application account.
- Combustible uses the same compact anchored month selector pattern as Mantenimiento: all twelve months appear in one vertical menu, the menu closes after selection, on focus leaving, or with Escape, and the selected period updates vehicle totals and the daily refuelling ledger.
- The Combustible vehicle selector mirrors Mantenimiento with five stacked horizontal banners in fixed fleet order. Each banner shows the vehicle number, real brand mark, brand, plate, model, and current-period fuel consumption on the right. Selecting a banner reveals that vehicle's refuellings sorted newest-first, with a receipt/invoice action at the far right of every row.
- When the Conductores button opens the driver analytics workspace, it shows compact full-width Facturación and Consumo summaries, followed by six borderless full-width driver rows without avatars or arrows; billing and consumption amounts stay prominent, and selecting a driver scrolls to that driver's calendar.
- Conductores keeps only euro amounts in the Consumo summary (no per-vehicle litres), uses bold enlarged vehicle plates and metrics, shows complete driver labels without clipping, and presents the selected calendar with only the driver name and month. The daily detail heading is omitted; Facturación/Repostaje panel headings and calendar-day amounts use enlarged type.
- Conductores driver rows show only the driver name, plate, billing amount, and consumption amount; the model and inline metric labels are hidden. Selecting a driver scrolls to the visible calendar header, calendar cells are compacted for a single-screen review, and horizontal month swipes follow the finger before changing month.
- Selecting a Toyota vehicle in the unified Vehículos workspace reveals both assigned drivers directly beneath its banner; particular vehicles remain without driver names.
- The particular Lexus 0344 LCP and Peugeot 9401 LTG do not display driver names or driver selectors in Flota or in the vehicle inspector; both are presented as vehicles without an associated driver.
- The SOBRE RUEDAS General screen opens with a combined four-colour summary chart for Facturación, Mantenimiento, Combustible, and Neto. Its grouped Vehículos button routes to the combined operational workspace; Mantenimiento opens its workspace and Neto retains its dedicated chart interaction. A Resumen control restores the combined chart, and a shared month and year selector updates the active chart and dashboard totals. The month menu keeps all twelve months in one vertical column, is right-aligned to its trigger, and uses only the width required by the month labels.
- The General chart metric control is an anchored dropdown with Resumen, Facturación, Mantenimiento, Combustible, and Neto options; Resumen is selected by default and combines all four series. Mantenimiento opens directly on its vehicle banners without the preventive-plan intro copy.
- The three-line management dropdown closes when the user clicks outside it, and its Facturas route button remains in the black global top bar next to the bell.
- The General home screen is a fixed no-scroll, no-zoom single-screen dashboard: the Conductores card has no arrow, Mantenimiento and Neto use compact uppercase title bands with a continuous Total/Por día/Por km metric row, and Mantenimiento is orange in the summary chart.
- On the General home screen, the Conductores card matches the compact height of Mantenimiento and Neto. The Resumen general por coche heading is uppercase and bold, and its Resumen, Mes, and Año controls use enlarged uppercase text across their available boxes without clipping.
- The three top-bar controls are compact, the three General dashboard buttons use a slightly wider desktop width and remain full-width on mobile, Mantenimiento uses orange, period menus present months and years as a vertical wheel with scroll snapping, and the Vehículos workspace omits the three bottom vehicle-driver summary cards.
- Month and year are selected through compact anchored in-page menus, never full-screen or browser-native pickers. The month menu shows all 12 months in one vertical column, the year menu uses a short list, and both close after selection, on focus leaving the control, or with Escape.
- The General Conductores card uses the same stacked layout as Mantenimiento and Neto: the uppercase title sits above full-width Facturación and Consumo metrics separated by vertical dividers.
- The Mantenimiento history keeps vehicle selection and invoice expansion intact while presenting interventions as a friendly visual timeline with clear dates, work concepts, highlighted amounts, and invoice states.
- Mantenimiento history headers show only the selected vehicle plate and total; the “Historial seleccionado” label and vehicle model/use line are omitted.
- Mantenimiento intervention history uses a compact orange timeline with a black entry heading and wrench icon; invoice rows keep date, kilometres, work concept, and invoice action readable in a reduced-height layout.
- General KPI cards keep permanent tone-colored borders: blue for Conductores, orange with a wrench icon for Mantenimiento, and green for Neto. Chart bar tooltips remain non-blocking, hide the active cursor overlay, and leave the other bars visible and tappable.
- The application uses a compact permanent dark charcoal bottom navigation bar with only three icon controls: home on the left, a cream plus centered horizontally and vertically, and user profile on the right. The home control returns to SOBRE RUEDAS > General, and page content reserves enough space for the General chart to remain fully visible above the bar.
- On the General home screen, the Conductores and Neto KPI cards use subtle tone-matched blue and green gradient fills alongside their icon and border colors.
- When a driver calendar is open on mobile, the previous summaries and driver list are hidden so the compact calendar, FacturaciÃ³n, and Repostaje panels fit together without scrolling; the calendar keeps adjacent-month pages in a finger-following swipe track before committing the new month.

- General summary, month, and year selectors use a cyclic three-copy wheel picker with a centered snap row and blurred adjacent choices; the fixed bottom bar centers the home and profile icons within their thirds without a normal button box, and maintenance history omits its helper instruction line.

- General home KPI cards use subtle flat fills without gradients. Tapping a chart bar adds only a pale gray category backdrop with no focus outline or active-bar highlight. The fixed bottom plus opens a horizontal Facturación/Consumo choice; selecting either category opens the native Android/iOS camera-or-files chooser directly, with no intermediate source menu or custom permission prompt.

- Facturación and Consumo bottom actions open the shared native camera-or-files chooser directly after category selection, validate and optimize images/PDFs, show cancellable progress/error states, and send documents only through the server-side `/api/analyze-document` endpoint. Structured AI extraction returns editable fields with per-field confidence; low-confidence values are highlighted before saving.

- The General chart's first menu is a flat uppercase checklist: Resumen restores all four series without a checkbox, while Facturación, Mantenimiento, Combustible, and Neto can be toggled in combination; month and year menus stay narrow and line-free.

- The General chart checklist opens with all individual checks empty while Resumen remains the default combined view; explicitly selected metrics fill their checks, change only the chart data, update the icon composition, and show uppercase titles without explanatory subtitles in a fixed chart layout.

- The fixed bottom plus preserves the Facturación/Consumo category choice and opens the native camera-or-files chooser directly from either handler. The selected category is retained for document validation and subsequent processing; the app does not render a Cámara/Subir archivo menu or a custom permission dialog.
- The General “Resumen general por coche” chart is presented as a compact clickable preview; selecting it opens a dialog with independent Facturación, Mantenimiento, Combustible, and Neto charts using their established colors and the active period. Selecting the Neto KPI opens the three-vehicle billing-and-expenses detail instead.
- The three General KPI buttons use the wider space created by the compact chart preview, with enlarged but non-clipping labels and amounts. When a driver is selected, Conductores switches to a fixed centered calendar view: the active month determines the calendar height, the lower blank area is removed, the driver name and period remain visible, and Facturación/Repostaje fit above the bottom navigation without page scrolling.
- Conductores calendar hierarchy uses large uppercase driver and period labels, wide blue Facturación and red Repostaje controls, enlarged day values, and enlarged lower detail metrics while preserving the fixed no-scroll viewport.
- The General dashboard's Conductores, Mantenimiento, and Neto buttons use 10% more desktop width, taking that space from the surrounding summary area while remaining full-width and non-overflowing on mobile.
- The General summary chart keeps all four metric buttons visible after selecting any metric, and renders each vehicle or driver total inside its corresponding colored bar with high-contrast vertical labels when space allows.
- Neto derives Taller from the selected period's Mantenimiento history and Gasolina from Combustible records. Nóminas are entered manually per period and driver, while Comisiones de conductores use a fixed 10% of each driver's monthly billing. Administración exposes an editable profile panel inside every professional vehicle accordion; profile name, email, vehicle assignment, and active state flow back into the operational views.

- The fixed bottom quick action uses the native camera-or-files chooser directly after Facturación/Consumo selection; the selected category and document continue into the existing validation, optimization, IA analysis, and review flow.
- The General dashboard lets the summary chart fill all remaining viewport space, enlarges its three KPI cards slightly, labels multi-metric selections in the chart trigger with metric initials separated by slashes, and compacts the mobile driver rows while preserving the FacturaciÃ³n/Consumo summary card sizes.
- The General summary preview uses separate bars and color semantics: FacturaciÃ³n is blue in the combined view and light blue when isolated, Mantenimiento orange, Combustible red, and Neto green. Its four compact color-coded controls remain visible below the chart and can toggle combinations; chart axis labels wrap to prevent vehicle plates and driver names from overlapping, while the KPI buttons receive the additional visual emphasis at the chart's expense.
- The General dashboard keeps KPI labels compact while enlarging only their numeric values; its “Resumen general por coche” heading is slightly reduced to leave room for the wider KPI row, and the default summary renders FacturaciÃ³n, Mantenimiento, Combustible, and Neto as separate side-by-side bars from the shared baseline for every vehicle.
- Mantenimiento opens each vehicle history with every intervention and concept detail collapsed by default. On the General home screen, the report top bar uses the same charcoal tone as the permanent bottom navigation, with slightly lighter controls.
- Mantenimiento integrates its real-time search into the first black top bar, sharing the row with the red SOBRE RUEDAS wheel and MANTENIMIENTO title. Search remains limited to existing maintenance records, shows at most five partial matches across vehicle, date, kilometre, concept, invoice, and provider fields, and selecting one switches vehicle and opens the exact intervention. The secondary search banner is omitted; vehicle banners use a white background with a black border, the next-service kilometres are prominent at double the prior size, uppercase titles are preferred in this workspace, and maintenance vehicle chevrons are omitted.
- Conductores keeps the two summary cards unchanged and distributes the six complete driver rows through the remaining viewport height, with equal vertical spacing and no hidden names or metrics before the fixed bottom navigation.

- Opening the General chart uses a fixed two-by-two detail surface showing only the four chart cards without page scrolling; the previous detail header is hidden and a floating X returns to the General dashboard.
- The administrator creates each driver's definitive password and is the only in-app actor with a password reset action; drivers do not receive a first-login password-change screen.
- Administration includes an “Entrar como conductor” preview for every active driver, with a clear return action to the administrator panel; the administrator can use the five daily capture controls and edit the five weekly monetary rows from that preview.
- Administration opens as a compact five-access list without the redundant hero or identity copy; its administrator row shows ADMINISTRADOR and DAVID DIAZ with matching hierarchy, and generated driver passwords appear in a narrow inline strip directly below that driver's reset actions.
- Administration opens with four accordion access buttons (administrator profile plus each professional vehicle), followed by a fifth create-access button; driver activation, pausing, password reset, and preview actions live inside the selected vehicle section.
- Cuando los acordeones de Administración están cerrados, sus cinco accesos (Administrador, tres coches y Crear nuevo acceso) se distribuyen en franjas iguales por toda la altura útil entre la cabecera y la navegación inferior; al abrir uno recupera el flujo de contenido necesario.
- Los cinco círculos de registro diario de la vista real del conductor y de su vista previa administrativa abren directamente el selector nativo de cámara/archivos; el documento se analiza con `/api/analyze-document` y, cuando la extracción es válida, actualiza el día correspondiente y el resumen semanal. Efectivo, Repostajes, Peajes, Lavados y Varios son editables desde celdas semanales del mismo tamaño, se muestran con formato de euros y se guardan en `driver_entries`; gasolina actualiza Repostajes y el efectivo cobrado extraído de Facturación actualiza Efectivo.
- Las capturas subidas desde Consumo alimentan con el valor OCR en l/100 km la gráfica semanal y la comparativa del conductor para el día asociado. Las capturas de Facturación actualizan el total diario facturado —y con ello el acumulado del banner mensual— además de Efectivo y Propinas en sus campos independientes.
- Conductores, Mantenimiento, Neto y Beneficio neto por coche no muestran importes, consumos, viajes, repostajes, intervenciones ni gastos económicos de demostración. Sus cálculos parten de cero y solo incorporan registros reales introducidos, documentos procesados, mantenimientos Funes autorizados o gastos manuales guardados.
- El reinicio solicitado de Mantenimiento y Neto invalida las facturas locales, intervenciones importadas, próximas revisiones y gastos manuales anteriores. Mantenimiento comienza sin historial ni total; Neto conserva los tres vehículos como estructura, pero todos sus conceptos e importes parten de cero.
- General summary bars use the Conductores blue as the default visual treatment; individual metric selections keep their established Facturación, Mantenimiento, Combustible, and Neto colors.
- The Neto detail opens with a black NETO header showing only the total, followed by three vehicle cards with enlarged amounts and prominent “Ver gastos” and “Añadir gastos” actions; expanding expenses reveals every concept.
- Conductores calendar pages omit the upper Facturacion/Repostaje legend and swipe helper; the freed height makes each day cell slightly larger while the lower Facturacion/Repostaje panels use more compact containers with enlarged values.
- The General chart keeps the Mes/Año period controls functional but hides their visible labels and compacts them into the upper row so the chart starts higher and its bars receive the released height.
- General KPI amounts and the vertical values rendered inside Facturación, Mantenimiento, Combustible, and Neto bars use a slightly larger readable scale without changing chart geometry.

- La vista móvil del conductor incorpora dos evidencias visuales de referencia, una de consumo y otra de facturación, dentro de sus tarjetas compactas; se identifican como ejemplos y no alteran los datos reales. Al abrirlas se muestran ampliadas, mientras que las lecturas reales siguen llegando desde los círculos de adjuntos y Supabase.
- En la aplicación de los conductores asignados, el DNI del propietario se muestra sin guiones y el resumen de Facturación y Propinas no incluye barras de progreso bajo sus importes.
- El registro diario de la vista del conductor contiene cinco accesos en orden fijo: Gasolina, Facturación, Km diarios, Km acumulados y Consumo. Cada acceso mantiene su imagen o símbolo contextual, abre la captura nativa y etiqueta el documento con su tipo para el análisis posterior.
- En las estadísticas del conductor, la tarjeta izquierda muestra la facturación mensual histórica de ese conductor en una tira horizontal seleccionable; la tarjeta derecha compara su consumo medio semanal con la media del resto de conductores.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

- La vista móvil de conductor sigue el modelo de referencia: cabecera azul marino con menú y notificaciones, acumulado mensual de facturación/propinas, registros diarios circulares con imágenes sustituibles, gráficos compactos, tabla semanal navegable y barra inferior de Inicio, Historial, Estadísticas y Ajustes. Los datos siguen viniendo de Supabase y el registro diario editable permanece accesible desde el menú del conductor.

- En la vista móvil del conductor, la acción principal de adjuntar parte de cada círculo de registro: el botón + inferior no se muestra. Gasolina, Parcial 1, Parcial 2 y Total abren el selector nativo de cámara/archivos y guardan el documento en Supabase con metadatos de lectura pendientes de análisis estructurado para su futura sincronización con Administración.

- Neto mantiene operativa la casita de la navegaciÃ³n inferior para volver al resumen; sus tres tarjetas profesionales usan una altura compacta por encima de la barra fija. En la vista del conductor se muestran el nombre y el DNI sin etiquetas, con el DNI destacado, y el mes en curso conserva su barra de facturaciÃ³n con propinas, peajes y otros gastos en formato ampliado.

- Conductores separa visualmente el importe de cada dia de su numero, con fechas negras y muy negritas. Mantenimiento mantiene la cabecera de vehiculo, intervenciones y total fija mientras se desplaza el historial naranja; sus fechas, kilometros, importes e iconos usan negro limpio con una barra inicial negra. Neto omite la franja duplicada de resultado de cada coche para que las tres tarjetas cerradas quepan sin scroll.
- El icono de perfil de la barra inferior abre directamente Administracion para el administrador; esa vista conserva sus cinco accesos, muestra conductores compactos seleccionables con sus acciones y la vista de conductor incluye kilometros diarios y kilometros totales del coche en controles contiguos.
- En General se mantiene una unica grafica en la propia pantalla: se eliminan el selector Resumen y la ventana secundaria de cuatro graficas, y Mes y Ano comparten el espacio disponible. En Mantenimiento, la cabecera del vehiculo queda fuera del scroll interno del historial para mantener matricula, intervenciones, taller e importe visibles al revisar sus facturas.
- En Neto, Gastos registrados usa la misma linea horizontal que Facturacion y el encabezado Gastos queda fijo mientras solo se desplazan los conceptos; Administracion se abre exclusivamente desde el perfil de la barra inferior.
- La vista de conductor muestra un registro mensual con facturacion acumulada, barra de progreso, propinas y peajes, y un registro semanal que calcula el efectivo cobrado menos gasolina, peajes y otros gastos imputables; estos cuatro importes se capturan y persisten en `driver_entries`.
- La vista de conductor incluye una tira horizontal continua con los días del mes seleccionado y el siguiente, permite cambiar de día sin scroll vertical y muestra el detalle diario con métricas y justificantes privados de Supabase, generando URLs firmadas solo para el conductor o el administrador.
- La vista de conductor agrupa el detalle del día y el registro editable en una única ficha con conceptos legibles; la bienvenida queda seguida por una barra mensual y, justo debajo, un registro semanal horizontal. La navegación inferior permanece fija y visible al abrir Conductores, Mantenimiento o Neto.
- El registro semanal de la vista del conductor se navega con un carrusel táctil sin flechas: la semana anterior, actual y siguiente permanecen enlazadas y el desplazamiento sigue el dedo antes de confirmar el cambio con una transición suave.
- El carrusel semanal confirma el cambio al terminar la transición real y conserva la semana visible durante el reajuste interno, evitando el salto posterior al deslizar a izquierda o derecha.
- El cambio de semana del conductor sincroniza en un único repintado la nueva semana y el recentrado interno del carrusel; la transición dura aproximadamente medio segundo, sigue el dedo y no presenta saltos ni reajustes visibles, tanto en la cuenta real como en la vista previa de Administración.
- Las etiquetas de las filas semanales (Efectivo, Repostajes, Peajes, Lavados, Varios y Total) usan una primera columna más ancha y tipografía grande para aprovechar toda su cuadrícula sin solaparse con los días.
- La vista previa del conductor abierta desde Administración no muestra la barra inferior de cuatro accesos ni los controles laterales de menú y notificaciones; conserva una cabecera azul compacta con nombre y matrícula centrados, y solo una flecha izquierda sin texto para volver al panel. Ese retorno no aparece en la cuenta real del conductor.
- La cabecera de Administración no muestra un menú de tres rayas a la izquierda del logo rojo; el retorno general se mantiene en su control específico.
- El Resumen general por coche conserva su diseño, sus datos visuales de referencia, las gráficas y los cuatro selectores inferiores permanentes de Facturación, Mantenimiento, Combustible y Neto; cada selector abre su gráfica correspondiente.
- Tras el reinicio económico solicitado el 14 de agosto de 2026, Facturación de conductores, importes de Mantenimiento, Combustible y todos los importes y gastos de Neto parten de cero; se mantienen intactos perfiles, matrículas, fechas, documentos y la estructura visual de gráficas y módulos.
- La cabecera azul de la vista del conductor muestra el nombre y la matrícula con la misma jerarquía tipográfica, también al abrirla desde Administración.
- La vista previa del conductor compacta el resumen para mostrar solo Facturación y Propinas en un bloque blanco con marco azul; oculta acumulado, propietario, DNI, ejemplos, aviso de solo lectura y encabezado semanal, y presenta doce barras mensuales de facturación sin scroll. Sus cinco tarjetas de captura y sus cinco importes semanales permanecen editables para el administrador.
- La tira horizontal del calendario del conductor muestra únicamente las celdas de los días y permite recorrer 13 meses de forma continua alrededor del periodo activo. Los selectores independientes de mes y año quedan fuera de la tira, usan menús verticales desplazables y permiten saltar a periodos anteriores.
- En Neto, Gasolina, Nóminas, Comisiones de conductores y Seguros sociales conservan su total agregado y se pueden expandir para revisar el importe individual de cada conductor, los repostajes asignados y la cuota del autónomo.
- La vista del conductor muestra junto a cada matrícula su propietario y DNI; la matrícula sigue siendo la clave de asociación de facturas, mantenimiento, combustible y gastos para que todos esos registros hereden el propietario correcto.
- La cabecera de Administración incluye un icono visible únicamente para el administrador que abre un selector de todas las vistas operativas y de gestión; la vista del conductor permanece como pantalla única sin navegación hacia las vistas de gestión.
- En el registro diario del conductor se muestra cada concepto debajo de su foto o símbolo, sin importes ni textos de ayuda visibles; la cabecera coloca nombre y matrícula en una banda azul compacta y la navegación semanal usa una transición larga y fluida.

The user has granted standing permission to answer “sí” to routine in-scope permission prompts for this project, including normal build and validation checks and, after successful validation, preparing commits containing only the files modified for the current functionality, pushing to the connected GitHub repository, and deploying to the linked Vercel production project without asking for confirmation each time. Unrelated, temporary, or generated files must remain excluded. Destructive actions, data deletion, or work outside the application scope still require confirmation.
